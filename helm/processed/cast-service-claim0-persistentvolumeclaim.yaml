apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.cast_service_claim0_persistentvolumeclaim.name }}
spec:
  accessModes:
    - {{ .Values.cast_service_claim0_persistentvolumeclaim.accessModes }}
  resources:
    requests:
      storage: {{ .Values.cast_service_claim0_persistentvolumeclaim.storage }}